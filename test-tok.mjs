import 'dotenv/config'; const auth = Buffer.from(process.env.RAZORPAY_KEY_ID + ':' + process.env.RAZORPAY_KEY_SECRET).toString('base64'); fetch('https://api.razorpay.com/v1/customers', { method: 'POST', headers: { Authorization: 'Basic ' + auth, 'Content-Type': 'application/json' }, body: JSON.stringify({ name: 'Agent', email: 'agent@test.com', contact: '9876543210' }) }).then(res => res.json()).then(cust => { fetch('https://api.razorpay.com/v1/customers/' + cust.id + '/tokens', { method: 'POST', headers: { Authorization: 'Basic ' + auth, 'Content-Type': 'application/json' }, body: JSON.stringify({ method: 'card', card: { name: 'Test', number: '4111111111111111', expiry_month: '12', expiry_year: '34', cvv: '123' } }) }).then(r => r.json()).then(tok => { console.log(tok); process.exit(0); }); });
