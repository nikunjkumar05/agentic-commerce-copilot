import 'dotenv/config'; import { query } from './api/_db.js'; import { v4 as uuidv4 } from 'uuid'; async function run() { const res = await query('SELECT id FROM users LIMIT 1'); if (res.rows.length === 0) return; const userId = res.rows[0].id; const now = new Date(); const invoices = [ { invNo: 'INV-2026-U001', name: 'Acme Corp', recipient: 'Acme Corp', amount: 13500, status: 'paid', date: new Date(now.getTime() - 1*86400000), is_ai_upsell: true }, { invNo: 'INV-2026-U002', name: 'Globex Inc', recipient: 'Globex Inc', amount: 15500, status: 'paid', date: new Date(now.getTime() - 4*86400000), is_ai_upsell: true }, { invNo: 'INV-2026-U003', name: 'Initech', recipient: 'Initech', amount: 9500, status: 'paid', date: new Date(now.getTime() - 10*86400000), is_ai_upsell: true }, { invNo: 'INV-2026-U004', name: 'Soylent Corp', recipient: 'Soylent Corp', amount: 23500, status: 'validated', date: new Date(now.getTime() - 14*86400000), is_ai_upsell: true } ]; for (const inv of invoices) { const check = await query('SELECT id FROM invoices WHERE invoice_number = ', [inv.invNo]); if (check.rows.length > 0) continue; const items = [ { description: 'Base Subscription', quantity: 1, unit_price: Math.round(inv.amount * 0.7 / 1.18), tax_rate: 18, total: Math.round(inv.amount * 0.7 / 1.18) }, { description: 'Agent Recommended Upsell', quantity: 1, unit_price: Math.round(inv.amount * 0.3 / 1.18), tax_rate: 18, total: Math.round(inv.amount * 0.3 / 1.18) } ]; const subtotal = items[0].unit_price + items[1].unit_price; const tax_total = Math.round(subtotal * 0.18); const grand_total = subtotal + tax_total; await query('INSERT INTO invoices (id, user_id, invoice_number, institution_name, institution_address, gst_number, recipient_name, recipient_address, recipient_gst, line_items, subtotal, tax_total, grand_total, currency, status, compliance_score, ai_suggestions, is_ai_upsell, invoice_date, due_date) VALUES (,,,,,,,,,,,,,,,,,,,)', [ uuidv4(), userId, inv.invNo, inv.name, inv.name + ', Delhi', '07AAACN0372J1ZB', inv.recipient, inv.recipient + ', New Delhi', '07BBBCD0483K2ZC', JSON.stringify(items), subtotal, tax_total, grand_total, 'INR', inv.status, 85, '[]', inv.is_ai_upsell, inv.date.toISOString().split('T')[0], new Date(inv.date.getTime() + 30*86400000).toISOString().split('T')[0] ]); } console.log('Done'); process.exit(0); } run();
