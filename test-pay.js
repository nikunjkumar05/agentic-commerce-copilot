const crypto = require('crypto'); require('dotenv').config(); const auth = Buffer.from(process.env.RAZORPAY_KEY_ID + ':' + process.env.RAZORPAY_KEY_SECRET).toString('base64'); fetch('https://api.razorpay.com/v1/payments', { method: 'POST', headers: { Authorization: 'Basic ' + auth, 'Content-Type': 'application/json' }, body: JSON.stringify({ amount: 1000, currency: 'INR', email: 'test@example.com', contact: '9876543210', method: 'card', card: { name: 'Test', number: '4111111111111111', expiry_month: '12', expiry_year: '34', cvv: '123' } }) }).then(res => res.json()).then(data => { console.log(data); process.exit(0); });
