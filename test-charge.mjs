import 'dotenv/config'; const auth = Buffer.from(process.env.RAZORPAY_KEY_ID + ':' + process.env.RAZORPAY_KEY_SECRET).toString('base64'); fetch('https://api.razorpay.com/v1/payments', { method: 'POST', headers: { Authorization: 'Basic ' + auth, 'Content-Type': 'application/json' }, body: JSON.stringify({ amount: 1000, currency: 'INR', email: 'agent@test.com', contact: '9876543210', customer_id: 'cust_TX5I59LYu39GtI', token: 'token_TX5I66pK0jRSG9' }) }).then(res => res.json()).then(data => { console.log(data); process.exit(0); });
